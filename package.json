{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "jest --config=./jest.config.json --coverage",
    "verbose": "jest --config=./jest.config.json --verbose",
    "coverage": "jest --config=./jest.config.json --coverage",
    "watch": "jest --config=./jest.config.json --watch",
    "watchAll": "jest --config=./jest.config.json --watchAll"

  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.20.2",
    "@babel/preset-env": "^7.20.2",
    "babel-jest": "^29.3.1",
    "jest": "^29.1.2",
    "jest-chain": "^1.1.6",
    "jest-extended": "^3.1.0"
  }
}